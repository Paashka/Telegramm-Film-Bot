🎬 Film & Series Tracker-Bot

Простой Telegram-бот помощник для совместного учета фильмов и сериалов.  
Позволяет добавлять, просматривать и удалять записи — всё через кнопки.  
Идеально подходит для ребят, которые не могут решить, что посмотреть 😅

> 💡Проект полностью рабочий. Написан на Python + `pyTelegramBotAPI`.  
> Данные хранятся в локальном JSON-файле — без PostgreSQL, чтобы было проще запустить.
> 
> Так же можно реалиализовать постоянный запуск через сервера гугл.

---

🌟 Возможности

- ➕ Добавить фильм или сериал в общий список  
- 📋 Посмотреть весь список (или последние 20, если он слишком длинный)  
- ❌ Удалить по порядковому номеру  
- 🎬/📺 Автоматическая категоризация: "Фильм" или "Сериал"  
- 📅 Отображение даты добавления

---

🛠 Технологии

| Компонент      | Инструменты                     |
|----------------|----------------------------------------|
| Язык           | Python 3.9+                            |
| Бот-фреймворк  | [`pyTelegramBotAPI`](https://github.com/eternnoir/pyTelegramBotAPI) |
| Хранилище      | `watchlist.json` (локальный файл)      |
| Архитектура    | Модульная: `handlers`, `database`, `keyboards` |

> 🔒 ВАЖНО: токен бота не добавлен в репозиторий! 
> В этом проекте он хранится в `config.py`.

---

⚙️ Расшерения

```bash
pip install pyTelegramBotAPI
```

---

⚡ Структура 
```bash
film-tracker-bot/
── config.py           Токен и константы
── main.py             Точка входа
── database.py         Работа с JSON
── handlers.py         Обработка команд
── keyboards.py        Генерация кнопок
── watchlist.json      Автоматически создается при первом запуске
